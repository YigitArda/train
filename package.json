{
  "name": "train-booking-flow",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/dev-server.mjs",
    "check": "npm test && pytest -q"
  }
}
